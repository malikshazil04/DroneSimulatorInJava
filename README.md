Project: Drone Fleet Coordination Simulator
Language: Java
This project simulates a group of drones moving in a 3D area.
It uses object oriented design.
Run instructions

compile
javac -d out $(find src/main/java -name "*.java")

run
java -cp out com.cepsim.core.Simulator
Configuration file is in src/main/resources/config.json.
